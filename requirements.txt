# ============================================================================
# AstrBot Desktop Client 依赖文件
# ============================================================================
#
# 这个文件仅用于向后兼容传统 pip 工作流。
#
# 推荐使用方式:
#   uv pip install -e .              # 使用 uv (推荐)
#   pip install -e .                 # 使用 pip
#
# 传统方式 (兼容性):
#   pip install -r requirements.txt
#
# ============================================================================
# 唯一真实来源: pyproject.toml
# 该文件依赖定义从 pyproject.toml 自动生成/同步
# 手动修改请直接编辑 pyproject.toml 的 [project.dependencies] 部分
# ============================================================================

# GUI 框架
PySide6>=6.5.0
qasync>=0.27.1

# HTTP 客户端
httpx[http2]>=0.24.0
httpx-sse>=0.4.0

# WebSocket 客户端
websockets>=11.0.0

# 屏幕截图
Pillow>=9.0.0
mss>=9.0.0

# 系统信息（用于桌面监控）
psutil>=5.9.0

# 全局快捷键
pynput>=1.7.0

# 配置管理
pydantic>=2.0.0

# 工具
python-dateutil>=2.8.0

# Markdown 渲染 & 代码高亮
markdown>=3.4.0
pygments>=2.15.0

# TOML 解析（Python < 3.11 需要）
tomli>=2.0.0; python_version < '3.11'

# ============================================================================
# 平台专用依赖
# ============================================================================

# macOS 平台专用依赖（用于窗口置顶等原生功能）
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"

# Windows 平台专用依赖（可选）
# 如需 Windows 专用功能（窗口信息获取等），请运行:
# pip install -e ".[windows]"
# 或手动安装: pip install pywin32>=306

# ============================================================================
# 开发依赖（可选）
# ============================================================================
# 如需开发工具（测试、代码格式化等），请运行:
# pip install -e ".[dev]"
